/** Same per-question timer budget the attempt page's CountDownTimer runs on
 * (daily-test-attempt/[courseId]/[testId]/[[...attempt]]/page.tsx) — the history list's
 * "Duration" stat is derived from this so the two screens never disagree. */
export const SECONDS_PER_QUESTION = 60;
